================================================================================
                    修仙模拟器 (xiuxian-v2) — 项目文件说明
================================================================================

【项目概述】
  基于 Next.js 16 + LangGraph + Prisma 的 AI 文字修仙游戏。
  前后端一体化架构，通过 SSE 流式返回 AI 生成的修仙剧情。
  玩家选择开局流派，AI 基于 LangGraph 状态机驱动剧情、数值结算、道具生成。

================================================================================
一、根目录配置文件
================================================================================

CLAUDE.md / AGENTS.md
  └─ Claude Code 的项目指令文件，告诉 AI 助手这是 Next.js 项目（非标准版本）

package.json
  └─ 项目依赖：Next.js 16, React 19, LangChain, Prisma, Zustand, Framer Motion 等

tsconfig.json
  └─ TypeScript 编译配置

tailwind.config.ts
  └─ Tailwind CSS 配置（新中式暗黑修仙风主题色）

components.json
  └─ Shadcn UI 组件库配置

prisma.config.ts
  └─ Prisma ORM 配置

prisma/schema.prisma
  └─ 数据库表结构定义。核心表：
     - Player：玩家数据（stats/inventory/codex/relationships 都是 JSONB 字段）
     - ChatMessage：对话历史
     - VectorDocument：pgvector 向量库文档

.env（需自行创建）
  └─ 环境变量：DATABASE_URL, OPENAI_API_KEY（或兼容 API 的 baseURL）

codex执行日志.txt
  └─ 上一轮 codex AI 助手的任务执行日志（图鉴系统改造）

================================================================================
二、src/app/ — Next.js App Router 页面与 API
================================================================================

src/app/layout.tsx
  └─ 全局根布局，加载字体、全局 CSS、metadata

src/app/page.tsx
  └─ 首页入口，根据 phase 状态切换 InitScreen / SelectScreen / 主游戏界面

src/app/globals.css
  └─ 全局样式，Tailwind 指令 + 修仙主题 CSS 变量

src/app/api/game/route.ts
  └─ GET 接口：读取玩家数据，返回 player + 对话历史

src/app/api/game/action/route.ts ★核心接口★
  └─ POST 接口：玩家发送动作/意图 → 调用 LangGraph 工作流 → SSE 流式返回
     流程：
       1. 鉴权：读 playerId，查 DB 获取 player
       2. 如玩家不存在：创建新玩家（带默认属性）
       3. 如向量库为空：注入世界观
       4. 构建 GameState 初始状态 → graph.invoke()
       5. SSE 流式下发 reply / codex / journal / step / done 事件
       6. 前端实时接收剧情文本 + 状态快照

================================================================================
三、src/lib/game/ — LangGraph 游戏引擎（后端核心）
================================================================================

graph.ts ★状态定义★
  └─ GameStateAnnotation：LangGraph 全局状态注解
     字段：messages, playerId, playerName, stats, inventory, codex,
           relationships, ragContext, intent, deltas, finalReply
  └─ buildGameGraph()：构建 4 节点有向图
     rag_retriever → plot_director → rule_engine → db_persist → END

nodes.ts ★节点实现★
  └─ ragRetrieverNode (Node A)：RAG 检索 → 从 pgvector 查世界观上下文
  └─ plotDirectorNode (Node B)：调用 LLM 生成剧情 + 工具调用
     - 绑定所有 gameTools，AI 可调用 Generate_NPC 等
     - 如果 AI 只调工具没输出文本，自动做二次补全
  └─ ruleEngineNode (Node C) ★纯代码规则引擎★
     - 处理 AI 的所有 tool_calls，更新 stats / inventory / codex / relationships
     - 护盾系统：伤害优先扣护盾，破盾后溢出扣HP；自动伤势分级判定
     - 动态灵力：Consume_Item 支持 mp_cost，玩家可自主注入1%~100%灵力
     - 支持的变更：Backpack_additems, Backpack_reduceitems, Consume_Item,
       Modify_Stats, Modify_Techniques, Modify_Traits,
       Modify_Mental, Update_Relationship, Change_Location,
       Check_Breakthrough, Generate_NPC/Location/Sect/Item (创建图鉴),
       Write_Codex, Write_Journal
  └─ dbPersistNode (Node D)：将 state 写入 PostgreSQL（Prisma）

prompts.ts ★AI 顶级提示词（system prompt）★
  └─ WORLD_RULE_PROMPT：修仙世界规则 + 写作风格 + 核心指令
  └─ directorPrompt：ChatPromptTemplate，拼接 system + 历史 + 用户输入
  └─ 注意：详细的属性变化规则不在这里，而在 tools.ts 各工具的 description 中

tools.ts ★AI 工具集 + 属性变化规则（per-field prompt）★
  └─ 17 个 DynamicStructuredTool（使用 Zod 校验参数）
  └─ ★关键设计★：每个工具的 description 字段就是该工具的 AI prompt，
     工具内每个参数的 description 约束该参数的行为。要修改某属性的规则，
     直接改对应工具对应字段的 description 即可。
  └─ 属性→工具→字段 映射表：
     心境(state_of_mind)  → Modify_Mental.state_of_mind_change  / Modify_Stats.state_of_mind_change
     气运(fortune)        → Modify_Stats.fortune_change
     业力(karma)          → Modify_Stats.karma_change
     声望(reputation)     → Modify_Mental.reputation_change     / Modify_Stats.reputation_change
     情绪(emotion)        → Modify_Mental.emotion
     HP/MP                → Modify_Stats.hp_change / mp_change / hp_max_change / mp_max_change
     护盾(shield)         → Modify_Stats.shield_change / shield_max_change
     功法(techniques)     → Modify_Techniques.main / add_combat / remove_combat / movement / add_support / remove_support
     天赋特质(traits)     → Modify_Traits.add_talents / remove_talents / add_traits / remove_traits
     境界/种族/阵营/宗门  → Modify_Mental.realm / race / alignment / sect / spiritual_root
     NPC关系              → Update_Relationship.npc_name / change
     位置                 → Change_Location.location
     突破                 → Check_Breakthrough.result / new_realm
     动态灵力消耗         → Consume_Item.mp_cost （支持1%~100%灵活注入）
  └─ 工具分类：
     - 世界生成：Generate_Item, Generate_NPC, Generate_Location, Generate_Sect
     - 背包操作：Backpack_additems, Backpack_reduceitems, Consume_Item
     - 属性变更：Modify_Stats, Modify_Techniques, Modify_Traits, Modify_Mental
     - 社交/移动：Update_Relationship, Change_Location
     - 系统：Check_Breakthrough, Skip, Write_Codex, Write_Journal

================================================================================
四、src/lib/ — 其他公共库
================================================================================

db.ts
  └─ Prisma 客户端单例（全局复用，防止开发时热重载创建多个连接）

vector-store.ts
  └─ pgvector 向量数据库操作：searchVectors, storeVector, injectWorldview
     注意：当前仅用于 RAG 世界观检索，图鉴数据已改为直接存 DB

utils.ts
  └─ Tailwind className 合并工具（cn 函数）

langgraph.ts
  └─ LangGraph 相关辅助（如有）

================================================================================
五、src/stores/ — 前端状态管理
================================================================================

game.ts ★Zustand 全局状态★
  └─ useGameStore：
     - player: 当前玩家数据（IPlayer）
     - chatHistory: 对话消息列表
     - codex: 图鉴条目列表（人物/地点/物品/宗门）
     - journal: 日志/事件条目
     - phase: INIT → SELECT → PLAYING → DEAD
     - currentView: 当前面板（chat/backpack/stats/journal/codex/settings）
     关键 Action：setPlayer, updateStats, addCodex, addJournal, resetGame
     持久化：localStorage（key: "xiuxian-game"）

================================================================================
六、src/types/index.ts — TypeScript 类型定义
================================================================================

  - GradeTier / GradeLevel / ItemGrade：品阶字面量
  - IntentType：AI 意图类型（NONE/REWARD/PENALTY/COMBAT）
  - IPlayer：玩家聚合根
  - ICharacterStats：角色全维面板（HP/MP/护盾/神识/境界/寿元/阵营/宗门/功法等）
  - IInventoryItem：背包物品
  - IRelationships：人际关系 Map
  - CodexEntry：图鉴条目（id/name/entry_type/description/metadata/timestamp）
  - JournalEntry：日志条目
  - IChatMessage：对话消息

================================================================================
七、src/components/ — React 前端组件
================================================================================

game-sidebar.tsx ★左侧导航栏★
  └─ 导航项：对话 / 背包 / 属性 / 日志 / 图鉴 / 设置
  └─ 移动端：汉堡菜单；桌面端：固定 72px 宽侧栏

chat-panel.tsx ★中控主屏 — 对话区★
  └─ 流式 SSE 渲染（打字机效果）
  └─ 输入框：自然语言 + "/" 快捷指令
  └─ 监听 SSE 事件：reply / codex / journal / step / error

status-panel.tsx ★右侧状态面板（桌面端）★
  └─ 精简信息：HP/MP/寿元进度条 + 神识 + 背包预览

stats-panel.tsx ★详细属性面板★
  └─ 全维数据：气血/灵力/寿元条、基础信息网格（境界/灵根/阵营/宗门等）
  └─ 功法、装备、天赋与特质展示

backpack-panel.tsx ★背包面板★
  └─ 物品列表，按品阶着色

codex-panel.tsx ★图鉴面板★
  └─ 按类型分组（人物/地点/物品/宗门），卡片式布局
  └─ 数据来源：store.codex ← setPlayer(player.codex) ← DB

journal-panel.tsx ★日志面板（经验库）★
  └─ 故事关键事件记录列表 + 4 个筛选标签（全部/成功经验/失败教训/踩坑点）
  └─ 每条日志有"聊这个"按钮，点击跳转聊天并预填内容

init-screen.tsx ★初始界面★
  └─ 输入道号（玩家名）、选择性别 → 调用 setPlayer → 进入 SELECT 阶段

select-screen.tsx ★流派选择界面★
  └─ 15 种开局流派（废柴流/退婚流/扮猪吃虎流...）
  └─ 选择后调用 POST /api/game/action（prepare 模式）生成开场剧情

settings-panel.tsx ★设置面板★
  └─ LLM 配置：API Key / Base URL / Model Name

ui/ — Shadcn UI 基础组件
  └─ button.tsx, input.tsx, label.tsx, progress.tsx, radio-group.tsx,
     scroll-area.tsx, sheet.tsx

================================================================================
八、数据流核心链路
================================================================================

【玩家创建 → 首次剧情】
  1. InitScreen：用户输入道号"齐尘" → setPlayer({name:"齐尘", stats:{...}, ...})
  2. SelectScreen：用户选择流派 → fetch POST /api/game/action
     body: { playerName:"齐尘", playerId, mode:"prepare", ... }
  3. route.ts：创建 DB Player（name:"齐尘", stats:默认属性）→ graph.invoke(init)
     init 包含 playerName:"齐尘"
  4. plotDirectorNode：LLM 看到 "Current Player: 齐尘" → 用这个名字写剧情
  5. LLM 调 Generate_NPC/Location 等工具 → ruleEngineNode 创建 codex 条目
  6. dbPersistNode 保存 stats/inventory/codex 到 DB
  7. SSE 流式返回剧情文本 + player 快照（含 codex）

【后续回合】
  1. ChatPanel：用户输入 → fetch POST /api/game/action
     body: { input:"走进山洞", playerId, mode:"play" }
  2. route.ts：读 DB player → graph.invoke(init with player data)
  3. 流程同上，playerName 从 DB 的 player.name 读取（始终保持一致）

================================================================================
九、关键注意事项
================================================================================

  - AI 提示词不要硬编码在 API 路由中，统一放 src/lib/game/prompts.ts
  - 所有数值变更由 ruleEngineNode（纯代码）处理，AI 只负责传参
  - API v1 SSE 事件类型：accepted / step / text-delta / codex / journal /
    state_update / completed / failed / cancelled（9种，判别联合 schema）
  - SSE 事件包装在版本化信封：protocolVersion, requestId, runId, sequence,
    occurredAt, type, payload
  - 终端事件（completed/failed/cancelled）每个流必须有且仅有一个
  - 错误响应用 RFC 9457 Problem Details 格式（application/problem+json）
  - 图鉴数据直接存 Player.codex (JSONB)，不再从向量库查询
  - 前端 Zustand store 持久化到 localStorage，避免刷新丢失

================================================================================
十、新增文件（API Robustness 重构 — 2026-07-23）
================================================================================

src/client/sse-parser.ts
  └─ 客户端增量 SSE 解析器（纯函数，无 React 依赖）
     parseSSEChunk / createSSEParser / parseSSEJson

src/client/game-turn-client.ts
  └─ API v1 HTTP 客户端（AsyncIterator），集成 SSEEventSchema 校验

src/client/game-turn-reducer.ts
  └─ 游戏回合状态机（7 状态：idle→submitting→streaming→completed/failed/cancelled）

src/components/error-boundary.tsx
  └─ React 渲染错误边界（ErrorBoundary + GameErrorBoundary）

src/app/error.tsx
  └─ Next.js 路由级错误页面（天道异常）

tests/fixtures/sse-protocol-fixtures.ts
  └─ SSE 协议测试固件（8 种场景：正常/失败/取消/损坏/缺序列/重复终端/中断）

openspec/changes/refactor-api-robustness/fault-injection-matrix.md
  └─ 故障注入矩阵（50 种故障场景，6 层架构）

src/server/application/execute-game-turn.ts
  └─ 游戏回合编排服务（单入口点，纯依赖注入）

src/server/application/game-graph.ts
  └─ LangGraph 工作流定义（4 节点：RAG → 剧情 → 规则引擎 → 持久化）

src/server/contracts/
  ├─ sse-events.ts       # SSE 事件判别联合 Schema
  ├─ problem-details.ts  # RFC 9457 错误 Schema
  ├─ player.ts           # 玩家状态 Schema
  └─ game-action.ts      # 游戏行动请求 Schema

src/server/streaming/
  ├─ sse-encoder.ts      # SSE 编码器
  ├─ sequence.ts         # 序列号分配器
  ├─ terminal-guard.ts   # 终端事件守卫
  ├─ event-factory.ts    # 事件工厂
  └─ adapter.ts          # 流适配器

src/server/domain/
  └─ 纯领域规则引擎（无副作用，依赖注入 now/random）
  └─ game-start.ts # 固定开局模板（穷散修初始背包/开场叙事；图鉴已清空，由世界生成填充）
  └─ world-gen.ts  # 世界生成系统：固定底层规则（WORLD_RULES 苍元大陆四域+正道七宗魔道三脉散修盟）
                      → LLM 调 GenerateLocation/Sect/Npc/AddCodexEntry 填充具体内容
                      → 存入 codex。首次 action 触发一次（needsWorldGenesis），
                      补全轮（needsGenesisCompletion）+ 固定模板兜底（buildFallbackWorld/buildFallbackBackground）。

src/server/infrastructure/
  └─ 仓储端口 + Prisma/Fake 适配器 + LLM/RAG/重试适配器

src/server/request-context/
  └─ 不可变请求上下文（requestId/runId/deadline/AbortSignal/clock/logger）

src/server/observability/
  └─ 结构化日志 + 递归敏感字段脱敏 + OpenTelemetry span

src/app/api/v1/
  ├─ game/action/route.ts   # API v1 游戏行动端点（SSE 流）
  └─ health/live|ready/     # 健康检查端点

src/app/api/game/action/route.ts
  └─ @deprecated 旧版端点，保留为兼容适配器（906 行，计划在 11.5 删除）

tests/
  ├─ unit/ (23 测试文件, 472 用例)
  ├─ contract/ (101 用例)
  └─ fixtures/ (共享测试固件)

================================================================================
十一、Agent框架重写 — 架构设计文件（2026-07-23 新增）
================================================================================

docs/architecture-brainstorm-log.md ★架构头脑风暴日志★
  └─ 14节完整设计文档，记录全部Agent框架重写的讨论过程
     包含：项目诊断、核心理念、NPC分级、知识气泡、动态约束、
           游戏模式、横向时间、Agent循环、工具五类、完整目录、
           Region DM协议、已完成vs待讨论

docs/开发计划-Agent框架重写.md ★开发计划★
  └─ 7阶段开发计划，覆盖从核心循环到前端对接的完整路线图
     当前状态：架构设计70%，代码实现5%

src/server/contracts/tool-catalog.ts ★工具目录（TypeScript）★
  └─ 25个工具的完整类型定义 + TOOL_REGISTRY + 辅助函数
     5大分类：感知查询(7) / 世界行动(13) / NPC行为(5) / 叙事输出 / UI面板
     扩展机制：metadata?: Record<string, unknown>

================================================================================
十二、Bug 修复验证（2026-07-31 新增）
================================================================================

e2e/bugfix-verify.spec.ts ★Bug修复e2e验证★
  └─ 真实LLM完整流程：验证面板切换不中断SSE流（CSS隐藏保持挂载）
     和 GenerateLocation 生成前校验（图鉴不重复新手村）

================================================================================
最后更新：2026-07-31
