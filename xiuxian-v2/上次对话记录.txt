上次对话记录 — 2026-07-31（Session 7 — 两 Bug 修复 + 项目评估 + Phase C 收尾）
================================================================================

【本轮任务】
1. 测试网站时发现两个 bug：切换面板导致 SSE 流中断（"Stream read cancelled"）；图鉴出现重复"新手村"条目
2. 用户要求理解 AI 决策链，评估项目上线进度，随后批准修复

【Bug 1 — 切换面板中断 SSE 流】
- 根因：page.tsx 条件渲染面板 → 切走时 ChatPanel 卸载 → useGameStream cleanup 调 abort() 杀掉 SSE 连接
- 修复：ChatPanel 改为始终挂载，CSS display 切换显隐（桌面 + 移动端）
- 用户观点："切界面它一样会存在的"（面板切换应保持状态/连接存活）

【Bug 2 — 图鉴重复"新手村"】
- 根因：种子地点只存在于 system prompt 文本（不在玩家 codex），LLM 每次探查都重新调用 GenerateLocation("新手村")，客户端 addCodex 无条件追加
- 用户关键反馈："在 AI 生成地点之前，就应该去做一个校验"（否决客户端去重方案，要求生成前服务端校验）
- 修复（TDD）：rule-engine.ts GenerateLocation 分支先查 SEED_LOCATION_NAMES + 已有 codex，命中跳过（记入 skippedLocations）；buildSystemPrompt【叙事之道】新增"已收录地点直接引用不重新生成"规则
- 5 个新单元测试：种子地点跳过 / 已有地点跳过 / 新地点添加 / 混合场景

【项目评估（用户问"离上线还差多少"）】
- 基础设施 ~85%：Agent Loop、25 工具、世界种子、NPC 三层（模板/决策/Agent）、Region DM、上下文压缩
- 预计 2-3 周（用 Claude 辅助），主要风险：LLM 试错 + 世界层行为树/记忆流仍空
- 下一步：Phase D — Agent 调用 Agent（玩家↔NPC 对话闭环）

【修复验证结果】
- type check：工作区 19 错误全部为历史遗留（基线对比确认零新增）；顺手修掉 npc-tick.test.ts 缺 beforeAll import
- vitest 948 用例：945 绿，3 失败（fault-injection SSE 契约测试，基线同样失败 = 预先存在）
- 数据库（Docker PostgreSQL 5433）未启动导致集成测试失败 → 启动 Docker + prisma db push 修复
- 真实 LLM e2e 验证脚本 e2e/bugfix-verify.spec.ts：完整流程验证两 bug（结果见会话）

【用户消息摘要】
- "我进去聊两句看看"（开始测试）
- "Stream read cancelled / 切到图鉴再切回来它就断了"
- "为什么会有两个一样的新手图呢？探查的时候突然又自己加了一个"
- "在AI生成地点之前，就应该去做一个校验"（修复方向的关键纠正）
- "当前这个AI到底是怎么决策的呢？"（要求解释 AI 决策链）
- "项目做到什么程度了？离上线还差多少？多少天？"（项目评估）
- "可以，开始修吧。顺便记得读取一下日志，还有一些记一下日志。"

【待办/下一轮】
- git commit（Phase C NPC 系列 + 本次两 bug 修复尚未提交，工作区脏）
- 3 个预先存在的 fault-injection SSE 契约测试失败待排查
- Phase D：Agent 调用 Agent（玩家→NPC 对话闭环）
================================================================================
