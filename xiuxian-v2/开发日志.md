# 修仙 — 开发日志

> 本文件是项目唯一权威的开发进度追踪文档。所有开发活动必须严格遵循此日志的规划。
> 设计细节参考 `docs/architecture-brainstorm-log.md` 和 `docs/开发计划-Agent框架重写.md`。
> 上次更新：2026-07-25（19:10）

---

## 零、产品定义

### 这是什么

一个**活的修仙世界模拟器**。不是聊天机器人，不是 AI 编剧，不是填表单的网页游戏。

你把你自己投入一个正在运转的修仙世界。这个世界里，NPC 有自己的性格、日程、私心和记忆。宗门在运作，坊市在交易，势力在角逐。你只是这个世界的一个参与者——甚至是一个干扰者。

你打字，世界回应你。但世界不围着你转。

### 核心设计信仰

1. **模拟器，不是舞台** — NPC 不等你点对话。状态机驱动日常行为，LLM 只在决策点唤醒
2. **知识气泡** — 你一个人在山洞里捡到功法，没有任何 NPC 应该知道。除非你告诉别人，或者有人看见了
3. **脑体分离** — LLM 是脑（高层决策），行为树/状态机是身体（日常执行）
4. **玩家是参与者，不是主角** — 你可以拜师、抢劫、欺骗、谈判、攻打宗门。但对方同不同意，是另一个 AI（或状态机）决定的
5. **Token 即成本，叙事即价值** — 没有废话。AI 的每个字都在推进剧情或揭示世界

### 参考作品

- 《觅长生》— 横向时间 + 闭关系统 + NPC 自主决策
- GTA V / RDR2 — 分区人口 + 五级优先级中断 + NPC 原型+参数变异
- 斯坦福 Generative Agents (Smallville) — 记忆流→检索→反思→规划
- Claude Code — Agent = for 循环，复杂度在世界层

---

## 一、架构全景

```
┌─────────────────────────────────────────────────┐
│                  玩家浏览器                        │
│    ChatPanel │ StatsPanel │ Backpack │ Codex     │
└──────────────┬──────────────────────────────────┘
               │ SSE (text-delta / state_update / step)
               ▼
┌─────────────────────────────────────────────────┐
│              API Gateway (Next.js)                │
│           POST /api/v1/game/action                │
└──────────────┬──────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────┐
│               Agent Loop (agent-loop.ts)          │
│                                                   │
│  Guard → Think → Act(loop) → Save                 │
│                                                   │
│  Plan-and-Execute:                                │
│    Planning Phase (no tools) → Execution Phase    │
│    (LLM + tools loop until done or hardLimit)     │
│                                                   │
│  Gate System (capabilityGate):                    │
│    工具校验 → 闸门检查 → 规则引擎 → 状态更新        │
└──────────────┬──────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────┐
│               世界层（World Layer）                 │
│                                                   │
│  RegionState — 区域约束 + 禁区 + 连通性            │
│  RuleEngine — 数值结算 / 背包 / 关系 / 状态        │
│  NpcEngine  — NPC 生成 + 状态查询                  │
│  WorldSeed  — 预种子世界数据（宗门/地点/NPC）       │
│  EntitySelector — 实体匹配（从种子数据选择）        │
└──────────────┬──────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────┐
│              PostgreSQL + pgvector                │
│    Player │ Inventory │ Codex │ NPC │ Tombstone  │
└─────────────────────────────────────────────────┘
```

### Agent 生命周期（GUARD → THINK → ACT → SAVE）

```
GUARD  防重 + 加载玩家 + 广播 accepted
THINK  RAG检索(可降级) + 复杂度估算 + 计划制定(简单输入跳过)
ACT    [循环] 组装上下文 → LLM推理 → 文本流输出 → 工具执行
       自修正: 工具校验失败不报废回合, 注入错误让LLM重试
       终止: LLM不调工具(done) / 达到轮次上限(hardLimit) / 取消
SAVE   构建快照 + 原子提交 + 广播 state_update/completed
```

---

## 二、实现状态总览

### 已完成

| 模块 | 核心文件 | 完成度 |
|------|---------|:-----:|
| Agent 循环引擎 | `agent-loop.ts` | 85% |
| Plan-and-Execute | `agent-loop.ts` (planning + execution) | 80% |
| 工具系统（25个工具定义） | `tool-catalog.ts`, `tool-schemas.ts` | 90% |
| Gate 闸门系统 | `agent-loop.ts` + `gate-checks.ts` | 70% |
| RegionState + Region DM | `region-state.ts` + `region-dm.ts` | 60% |
| 规则引擎 | `rule-engine.ts` | 70% |
| NPC 引擎 | `npc-engine.ts` + `npc-activity.ts` | 55% |
| **NPC 原型系统**（GTA V式） | `npc-archetype.ts` | 90% |
| 知识气泡 | `knowledge-bubble.ts` | 30% |
| 世界种子数据 | `world-seed.ts` | 80% |
| 实体选择器 | `entity-selector.ts` | 60% |
| 上下文压缩 | `context-compression.ts` | 70% |
| SSE 流式输出 | `route.ts` + `event-factory.ts` | 80% |
| 前端 UI 面板 | Chat/Stats/Backpack/Codex/Status | 70% |
| GM Prompt（身份驱动） | `agent-loop.ts` buildSystemPrompt | 90% |
| 测试 | 单元 650 + E2E 214 + 契约 34 | 80% |

### 未开始（按优先级排列）

| # | 模块 | 说明 |
|---|------|------|
| 1 | **竖切实验** | ✅ 已完成 — 3 NPC + 日程 + 知识气泡 + Agent Loop集成 |
| 2 | **Region DM 完整协议** | 6个职责：人口层 + 活动分配 + 约束生成(LLM) + 事件编排(LLM) + 优先级裁决 + 后果追踪 |
| 3 | **NPC 行为树引擎** | 脑体分离 — 状态机身体执行日计划，LLM 脑只在决策点唤醒 |
| 4 | **NPC 记忆流存储** | 外部记忆流（非 LLM 上下文），检索→反思→规划 |
| 5 | **横向时间推进** | 玩家行动消耗时间 → 区域 NPC 统一 tick → 检查点保存 |
| 6 | **知识气泡升级** | NPC 只能感知周围，约束由 Region DM 编译 |
| 7 | ~~约束注入 + bound 机制~~ | ✅ 已完成 — NPC原型+参数 → trait覆盖 + lawfulness概率反叛 → 确定性绑定 |
| 8 | **NPC 动态升降级** | T1(模板) ↔ T2(混合) ↔ T3(完整Agent)，按交互频率切换 |
| 9 | **双模型架构** | 快速模型做分类判断 + 强力模型做叙事生成 |
| 10 | **百世书/轮回系统** | 死亡→墓碑→新角色继承遗物/遗泽/遗恨 |
| 11 | **前端 Agent 可视化** | 工具调用展示、NPC 状态面板、世界时钟 UI |

### 当前最大债务

**世界层有基础骨架，但行为树和记忆流仍为空。** 我们有一个能跑通的 Agent 循环，能调工具、能过闸门、能流式输出文本。但 NPC 没有行为树，没有日计划，没有知识气泡，没有记忆流。`region-state.ts` 只是一个数据查询单例，不是设计文档里描述的 Region DM。

我们现在的状态：**Agent 循环是一个精美的壳，里面没有世界。**

---

### 近期完成（2026-07-25）

| 日期 | 内容 | 详情 |
|------|------|------|
| 07-25 | **GM Prompt 身份驱动重写** | 从"根据玩家输入推进剧情"→"世界运转者"。3个prompt函数重写，`context-assembly.ts`标记deprecated。Wiki研究驱动设计。 |
| 07-25 | **Agent 生命周期重构** | 12步→4阶段（GUARD→THINK→ACT→SAVE），修复3个缺口：hardLimit无声、工具校验失败报废回合、全部工具被阻止无限循环。自修正校验+熔断器。 |
| 07-25 | **NPC/世界层审查** | `npc-engine.ts`只有T1模板NPC，无行为树/无日计划/无记忆流。`region-state.ts`只是数据查询单例。`world-seed.ts`有6宗门6地点15物品8功法。世界层确认空壳。 |
| 07-25 | **竖切实验完成** | 3个预种子NPC（王老四T2店主/李散修T1路人/张铁匠T1路人）+ NPC日程活动系统 + 知识气泡 + Agent Loop集成。35个新测试全绿，581个存量测试未破坏。 |
| 07-25 | **Region DM + NPC原型系统** | 4个NPC原型（商人/散修/匠人/守卫）含行为池+参数范围；GTA V式参数随机roll（贪婪/胆量/守法度等）；青云坊市7条约束规则；约束绑定引擎（trait覆盖 + lawfulness概率反叛 + 确定性hash）；`createNpcFromSeed`接入原型系统。69个新测试全绿，存量650测试未破坏。 |

---

## 三、下一步计划

### 立即：将 Region DM + NPC系统接入Agent Loop

**目标**：让NPC的约束绑定和原型参数真正影响LLM行为。

**范围**：
- Agent Loop的NPC上下文注入中加入约束格式化文本（`formatConstraintContext`）
- 系统提示中的NPC展示加入约束违规摘要（供GM参考）
- gate checks 中加入区域规则参考
- NPC对话匹配中加入原型对话风格注入

### 后续：NPC行为树引擎

**目标**：脑体分离 — 状态机身体执行日计划，LLM脑只在决策点唤醒。

### 后续：横向时间推进

玩家行动消耗时间 → 区域NPC统一tick → 检查点保存。
- 知识气泡是否生效（店主不知道玩家之前做了什么）
- 玩家是否能自由选择交互方式（正常交易 / 砍价 / 抢劫）

### 后续（Phase 1-4）

严格按 `docs/开发计划-Agent框架重写.md` 执行：
1. Phase 2: 世界层（Region DM + 横向时间 + 行为树引擎）
2. Phase 3: 工具系统完善
3. Phase 4: NPC 系统（模板 + 行为树 + 记忆流 + 日计划）
4. Phase 5: 竖切实验（完整闭环）
5. Phase 6: 优化（双模型 + 上下文压缩 + 缓存）
6. Phase 7: 前端对接

---

## 四、开发规则

1. **先读本日志** — 每次开始工作前，先读本文件确认当前进度和优先级
2. **设计文档是真理** — `docs/architecture-brainstorm-log.md` 和核心技术架构文档包含了完整设计，实现前先对照
3. **不改无关代码** — 只改任务要求的部分，不"顺便优化"
4. **测试先行** — 任何逻辑变更先写测试，红→绿→重构
5. **完成即更新本日志** — 每完成一个模块，更新上面的实现状态表
6. **竖切优先** — 不做"全部设计完再动手"，做"最小可用→验证→迭代"
